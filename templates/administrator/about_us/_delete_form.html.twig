<form method="post" action="{{ path('app_administrator_about_us_delete', {'id': about_u.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ about_u.id) }}">
    <button class="btn">Delete</button>
</form>
